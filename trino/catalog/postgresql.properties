connector.name=postgresql
connection-url=jdbc:postgresql://chu_postgres_data:5432/healthcare_data
connection-user=admin
connection-password=admin123

# Performance (Trino 435+ - propriétés obsolètes supprimées)
# postgresql.experimental.connection-pool-enabled → toujours activé par défaut
# query.pushdown.enabled → remplacé par des options spécifiques
